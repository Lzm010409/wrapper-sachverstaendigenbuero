import { SkelettFallseite } from '@/app/teile/skelett'

/**
 * Die Fallseite hat einen Kopf, eine Reiterleiste und einen Reiter. Der
 * Platzhalter bildet alle drei nach — so steht die Seite sofort in ihrer
 * endgültigen Form da, und nichts springt, wenn der Inhalt kommt.
 */
export default function Laedt() {
  return <SkelettFallseite />
}
